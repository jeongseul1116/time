# 자동 조율 프로토타입 — 화면 흐름

`index.html` 한 파일로 된 프로토타입의 모든 화면을 순서대로 옮긴 문서입니다.
모든 문구는 소스에서 그대로 뽑았습니다. 변수로 채워지는 자리는 `{ }` 로 표시하고, 바로 아래에 실제 예시를 붙였습니다.

기준 데이터
- 오늘 = 2026-07-30 (`TODAY`)
- 조율 대상 주 = 2026-08-03(월) ~ 08-07(금) (`BASE_MON`)
- 표시 시간대 = 오전 9시 ~ 오후 6시 (`START_H = 9`, `END_H = 19`)
- 점심 = 12~13시 (`LUNCH`), 후보에서 제외됨

---

## 1. 첫 화면 — 캘린더 (주 보기)

페이지를 열면 바로 이 화면입니다. 별도의 시작/로그인 화면은 없습니다.

### 1-1. 상단바

| 요소 | 화면에 뜨는 문구 | 비고 |
|---|---|---|
| 좌측 아이콘 | `☰` | `title="기본 메뉴"` |
| 로고 | `30` + `Calendar` | 로고 사각형 숫자는 `TODAY.getDate()` = 30 |
| 버튼 | `오늘` | |
| 이전/다음 | `‹` / `›` | `title="이전 주"` / `title="다음 주"` |
| 기간 제목 | `2026년 8월` | 보기에 따라 바뀜 (§8) |
| 검색 | `🔍` | `title="검색"` |
| 검색 입력 | placeholder `일정 검색` | 열렸을 때만 보임 |
| 검색 지우기 | `✕` | `title="검색 지우기"` |
| 둘러보기 | `▶ 둘러보기` | |
| 지원 | `?` | `title="지원"` |
| 설정 | `⚙` | `title="설정"` |
| 보기 | `주 ▾` | 현재 보기 이름 + `▾` |
| 계정 | `정` | 아바타 |

### 1-2. 사이드바

**만들기 버튼**

```
＋ 만들기 ▼
```

처음에는 파란 링이 깜빡입니다(`class="ring"`). 한 번 누르면 링이 사라집니다.

**미니 캘린더**

```
2026년 8월      ‹  ›
월 화 수 목 금 토 일
```

**사용자 검색**

```
👤  [사용자 검색]
```

검색 결과가 없을 때: `검색 결과가 없습니다.`

**내 캘린더** (`내 캘린더` + `⌃`)

사람 목록 16명이 색 체크박스와 함께 나옵니다. 각 줄에 마우스를 올리면 우측에 `이 캘린더만` 이 나타납니다.

| 이름 | 역할 | 이메일 |
|---|---|---|
| 정슬기 | 주최자 | seulgi@company.co.kr |
| 김도현 | 프로덕트 | dohyun@company.co.kr |
| 박서연 | 디자인 | seoyeon@company.co.kr |
| 이준호 | 개발 | junho@company.co.kr |
| 최민아 | 마케팅 | mina@company.co.kr |
| 한지우 | 데이터 | jiwoo@company.co.kr |
| 강태섭 | 대표 | ceo@company.co.kr |
| 오하진 | 백엔드 | hajin@company.co.kr |
| 윤소정 | 프론트엔드 | sojung@company.co.kr |
| 장민규 | QA | mingyu@company.co.kr |
| 서현수 | 세일즈 | hyunsoo@company.co.kr |
| 남보영 | CS | boyoung@company.co.kr |
| 권재이 | 인사 | jaei@company.co.kr |
| 임태호 | 재무 | taeho@company.co.kr |
| 배다율 | 리서치 | dayul@company.co.kr |
| 고승원 | 법무 | seungwon@company.co.kr |

**다른 캘린더** (`다른 캘린더` + `⌃`)

```
✓ 대한민국의 휴일
✓ 회의실 예약
```

### 1-3. 주간 그리드

```
GMT+09   월 3   화 4   수 5   목 6   금 7
종일     [휴가 · 한지우]        [전사 워크숍]
오전 9시
오전 10시
오전 11시
오후 12시   ← 점심 (빗금)
오후 1시
…
오후 6시
```

- 시간 라벨: `오전 9시` … `오후 12시` … `오후 6시`
- 종일 행 라벨: `종일`
- 일정 블록: 제목 + 아래 줄에 `{주인 이름}` 또는 `{주인 이름} 외 {N}명`
  - 예: `스프린트 계획` / `김도현 외 2명`
- 30분짜리 일정은 제목만 표시(아래 줄 없음)

### 1-4. 분기

| 조작 | 결과 |
|---|---|
| `만들기` 클릭 | §2 만들기 메뉴 열림 |
| 그리드 빈 칸 클릭 | §3 빈 칸 팝오버 |
| 일정 블록 클릭 | §4 일정 상세 팝오버 |
| 종일 칩 클릭 | §5 종일 일정 팝오버 |
| `▶ 둘러보기` 클릭 | §11 둘러보기 시작 |
| `‹` `›` | 현재 보기 단위만큼 이동 (일/4일/주/월/연도/21일) |
| `오늘` | 2026-07-30 이 있는 주로 이동 |
| `주 ▾` | §9 보기 메뉴 |
| `?` / `⚙` / `정` | §10 상단바 메뉴들 |
| `☰` | 사이드바 접기/펼치기 |
| `내 캘린더` / `다른 캘린더` 헤더 | 해당 목록 접기/펼치기 |
| 사람 줄 클릭 | 그 사람 캘린더 숨김/표시 |
| `이 캘린더만` 클릭 | 그 사람만 남기고 전부 숨김 (이미 혼자면 전부 표시로 되돌림) |
| 사용자 검색에서 사람 선택 | 그 사람만 남기고 전부 숨김 |
| 일정 검색어 입력 | 일치하는 일정에 파란 테두리, 나머지는 흐려짐 |

---

## 2. 만들기 메뉴

`만들기` 버튼 아래로 펼쳐집니다.

```
일정
할 일
약속 일정
─────────────
자동 조율        [New]
```

- `일정`, `할 일`, `약속 일정` 은 클릭 핸들러가 없습니다. 눌러도 아무 일이 일어나지 않습니다.
- `자동 조율` → 보기를 `주`로 바꾸고 2026-08-03 주로 이동한 뒤, 우측 시트를 열어 §6 으로 갑니다.
- 메뉴 바깥을 클릭하면 닫힙니다.

---

## 3. 빈 칸 팝오버

그리드의 빈 슬롯을 클릭하면 뜹니다.

```
                                   ✕

이 시간에
{8월 3일 (월요일) · 오전 9:00 – 오전 10:00}

[ 일정 만들기 ]   [ 자동 조율 ]

자동 조율은 이 시간을 고집하지 않습니다. 참석자를 넣으면
다음 주 전체에서 잡을 수 있는 시간을 순위로 찾아옵니다.
```

| 조작 | 결과 |
|---|---|
| `일정 만들기` | 팝오버만 닫힘 |
| `자동 조율` | 팝오버 닫고 `자동 조율`을 실행 → §6 |
| `✕` 또는 바깥 클릭 | 닫힘 |

---

## 4. 일정 상세 팝오버

일정 블록을 클릭하면 뜹니다.

```
              ☰   🗑   ✉   ⋮   ✕

■  {스프린트 계획}
   {8월 3일 (월요일) · 오후 2:00 – 오후 3:30}

👥  참석자 {3}명
    {김도현} · {프로덕트}
    {이준호} · {개발}
    {박서연} · {디자인}
    {김도현}님이 만든 일정 · {김도현}님 캘린더에서 보는 중

🔔  10분 전 알림
📍  회의실 미지정
```

도구 버튼 툴팁: `☰`=`옵션`, `🗑`=`삭제`, `✉`=`메일 보내기`, `⋮`=`더보기`, `✕`=`닫기`

조건에 따라 달라지는 부분

| 조건 | 표시 |
|---|---|
| 일정 길이 ≥ 1시간 | `10분 전 알림` |
| 일정 길이 < 1시간 | `알림 없음` |
| 제목에 `집중` 또는 `개인` 포함 | `🔒 바쁨 · 비공개` |
| 그 외 | `📍 회의실 미지정` |
| 거절자가 있는 일정 | 날짜 줄 끝에 ` · {정슬기} 거절` 이 붙고, 그 사람 줄에 `거절함` 태그 + 취소선 |
| 자동 조율 중(참석자 1명 이상 선택됨)일 때 | 참석자 줄에 `필수` 또는 `선택` 태그가 붙음 |

거절 데이터가 들어 있는 일정 (다음 주 기준)
- 화 11–12 `제품 교육` — 정슬기 거절
- 목 16–17 `전사 공유회` — 정슬기, 김도현 거절
- 금 10–11 `보안 교육` — 정슬기 거절

---

## 5. 종일 일정 팝오버

종일 행의 칩을 클릭하면 뜹니다.

```
              ☰   🗑   ✉   ⋮   ✕

■  {전사 워크숍}
   {8월 7일 (금요일) · 종일}

📅  회사 일정
👥  전 직원
```

종일 항목 목록 (`ALLDAY_POOL`)

| 제목 | 막는 범위 |
|---|---|
| 전사 워크숍 | 그 날 전체 |
| 휴가 · 한지우 | 한지우만 |
| 창립기념일 | 그 날 전체 |
| 전사 오프사이트 | 그 날 전체 |
| 휴가 · 박서연 | 박서연만 |

조율 대상 주(2026-08-03)에는 목요일 `휴가 · 한지우`, 금요일 `전사 워크숍` 두 개가 고정으로 뜹니다.

자동 조율 중에는 칩 앞에 `🔒` 가 붙고, 그 날 전체가 막힌 날은 요일 칸 위에 세로 글씨로 다음이 덮입니다.

```
{전사 워크숍} · 조율 불가
```

---

## 6. 시트 1단계 — 인원 지정

우측 시트가 열립니다. 헤더:

```
자동 조율                                      ✕
AI가 참석자와 필수 / 선택을 뽑고, 주최자가 고칩니다.
```

### 6-1. 초안 복구 배너 (조건부)

새로고침 전에 제목이나 참석자를 입력해 둔 상태였다면 맨 위에:

```
새로고침 전에 작성하던 내용을 불러왔습니다.  [ 새로 시작 ]
```

`새로 시작` → 제목·참석자·링크를 모두 비우고 같은 화면을 다시 그림.

### 6-2. 링크 블록 — 상태 A: 입력 전

```
슬랙 스레드 · 노션 링크   [AI 분석]

[ 링크를 붙여넣으면 참석자를 뽑습니다 ]   [ 분석 ]

데모용 링크 — 클릭하면 바로 분석합니다
`슬랙 스레드`   `노션 문서`
```

| 조작 | 결과 |
|---|---|
| `분석` 클릭 / 입력창에서 Enter | 상태 B로 |
| 입력창에 `http(s)://` 로 시작하는 텍스트 붙여넣기 | 즉시 상태 B로 |
| `슬랙 스레드` 코드칩 클릭 | 슬랙 링크로 상태 B |
| `노션 문서` 코드칩 클릭 | 노션 링크로 상태 B |

링크 종류 판정: URL에 `slack.com` 또는 `slack://` → 슬랙 / `notion.so` 또는 `notion.site` → 노션 / 그 외 → 일반 링크

### 6-3. 링크 블록 — 상태 B: 분석 중

```
{슬랙 스레드} 분석 중   [AI]

▓▓▓▓▓░░░░░░░░░░
✓ 스레드 메시지 34개 읽는 중
✓ 발언자와 멘션 대상 구분
✓ 질문을 직접 받은 사람 식별
✓ 필수 / 선택 배분

                                    [ 취소 ]
```

라벨은 종류별로 `슬랙 스레드` / `노션 문서` / `링크` 중 하나입니다.

진행 단계 문구 (종류별)

**슬랙**
1. `스레드 메시지 34개 읽는 중`
2. `발언자와 멘션 대상 구분`
3. `질문을 직접 받은 사람 식별`
4. `필수 / 선택 배분`

**노션**
1. `문서 본문과 속성 읽는 중`
2. `담당자 · 승인자 필드 확인`
3. `미해결 코멘트 작성자 추출`
4. `필수 / 선택 배분`

**일반 링크**
1. `링크 본문 가져오는 중`
2. `등장 인물 추출`
3. `역할 추정`
4. `필수 / 선택 배분`

한 단계당 520ms, 4단계가 끝나면 자동으로 상태 C가 됩니다.
`취소` 를 누르면 상태 A로 되돌아갑니다.

### 6-4. 링크 블록 — 상태 C: 분석 완료

```
분석한 링크   [AI 제안 반영됨]

[#]  {#product-roadmap 스레드}                        ✕
     {메시지 34개 · 참여자 6명 · 미결 질문 3건}
```

`✕` 툴팁은 `링크 제거`. 누르면 링크가 빠지고, AI가 넣었지만 주최자가 손대지 않은 참석자도 함께 빠집니다.

종류별 카드 내용

| 종류 | 아이콘 | 제목 | 요약 |
|---|---|---|---|
| 슬랙 | `#` | `#product-roadmap 스레드` | `메시지 34개 · 참여자 6명 · 미결 질문 3건` |
| 노션 | `◈` | `3분기 로드맵 초안` | `코멘트 12개 · 미해결 4건 · 담당자 1명 · 승인자 1명` |
| 일반 | `🔗` | `붙여넣은 링크` | `본문에서 이름 4개 · 결정 요청 2건 추출` |

분석이 끝나면 회의 제목이 비어 있을 때만 자동으로 채워집니다.

| 종류 | 자동 입력되는 제목 |
|---|---|
| 슬랙 | `3분기 로드맵 우선순위 정렬` |
| 노션 | `3분기 로드맵 초안 리뷰` |
| 일반 | `논의 마무리 미팅` |

### 6-5. 회의 제목

```
회의 제목
[ 예: 3분기 로드맵 정렬 ]
```

입력할 때마다 자동 저장됩니다(하단 저장 표시가 잠깐 `저장 중…` 으로 바뀜).

### 6-6. 참석자 추가

```
참석자 추가
[ 이름 또는 이메일 검색 ]
```

입력하면 아래로 결과 목록이 뜹니다. 이름·이메일·역할로 검색됩니다.

```
(아바타)  {김도현} · {프로덕트}
          {dohyun@company.co.kr}
```

- 이미 추가된 사람은 이메일 뒤에 ` · 추가됨` 이 붙고 클릭할 수 없습니다.
- 결과가 없으면: `검색 결과가 없습니다.`
- 선택하면 `필수` 로 추가됩니다.

### 6-7. 참석자 목록

라벨:

```
필수 {3}명 · 선택 {3}명   [AI 제안]
```

`AI 제안` 태그는 AI가 넣은 사람이 한 명이라도 있을 때만 붙습니다.

**빈 상태**

```
아직 참석자가 없습니다. 링크를 붙여넣거나 위에서 검색해 추가하세요.
```

**참석자 한 줄**

```
(아바타)  {김도현} · {프로덕트}  [재조율 어려움]
          {dohyun@company.co.kr}
          ✦ {스레드를 열고 결정을 요청함}  [주최자 수정]
                                  [필수][선택]   ✕
```

- `재조율 어려움` — 강태섭(대표)에게만 붙습니다.
- 근거 줄: AI가 넣은 사람은 `✦ {근거}`, 주최자가 직접 검색해 넣은 사람은 `주최자가 직접 추가`
- `주최자 수정` — AI가 정한 필수/선택을 주최자가 바꾼 줄에만 붙습니다.
- `✕` 툴팁은 `제외`.

**AI가 넣는 참석자와 근거 — 슬랙 링크**

| 이름 | 배분 | 근거 |
|---|---|---|
| 김도현 | 필수 | `스레드를 열고 결정을 요청함` |
| 이준호 | 필수 | `일정 산정 질문을 직접 받음 · 미응답` |
| 박서연 | 필수 | `디자인 리소스 확정이 스레드 안건` |
| 최민아 | 선택 | `멘션으로 언급만 됨` |
| 한지우 | 선택 | `이모지 반응만 남김` |
| 오하진 | 선택 | `작업 범위를 한 번 답변함` |

**노션 링크**

| 이름 | 배분 | 근거 |
|---|---|---|
| 김도현 | 필수 | `담당자(Owner) 필드에 지정됨` |
| 한지우 | 필수 | `미해결 코멘트 4건을 남김` |
| 강태섭 | 필수 | `승인자 필드에 지정됨` |
| 박서연 | 선택 | `열람 기록만 있음` |
| 윤소정 | 선택 | `코멘트 1건 · 이미 해결됨` |

**일반 링크**

| 이름 | 배분 | 근거 |
|---|---|---|
| 김도현 | 필수 | `본문에서 결정을 요청받음` |
| 이준호 | 필수 | `작업 항목이 배정됨` |
| 박서연 | 선택 | `본문에 이름만 등장` |

**AI 배분 안내 배너** (AI가 넣은 사람이 있을 때만)

```
AI가 뽑은 배분입니다. 필수 / 선택은 주최자가 바로 바꿀 수 있고,
바꾼 줄은 “주최자 수정”으로 표시됩니다.
```

**재조율 어려움 경고 배너** (강태섭이 참석자에 있을 때만)

```
참석자 중 재조율이 어려운 사람이 있습니다.
조율 요청에 “놓치면 다시 잡기 어렵다” 표시가 함께 나갑니다.
```

### 6-8. 하단

```
자동 저장됨 · 새로고침해도 유지            [ 가능한 일정 뽑기 ]
```

저장이 일어난 직후 0.5초 동안은 `저장 중…` 으로 바뀝니다.

### 6-9. 이 화면에서 캘린더가 바뀌는 것

참석자를 한 명이라도 고르면
- 사이드바 `내 캘린더` 목록이 **이 회의 참석자만** 남습니다.
- 그리드에도 참석자의 일정만 남습니다.
- 종일 칩에 `🔒` 가 붙고, 전사 일정이 있는 날은 `{전사 워크숍} · 조율 불가` 로 통째로 덮입니다.

### 6-10. 분기

| 조작 | 조건 | 결과 |
|---|---|---|
| `가능한 일정 뽑기` | 필수 인원 1명 이상 | §7 스캔 화면 |
| `가능한 일정 뽑기` | 필수 인원 0명 | 버튼이 비활성 상태라 눌리지 않음 |
| `✕` (시트 헤더) | | 시트 닫힘, 초안 삭제, 캘린더로 복귀 |
| `Esc` | | 시트 닫힘 (위와 동일) |

---

## 7. 시트 2단계 — 스캔 중

```
가능한 일정 찾는 중
(부제 없음)

              ◠ (도는 원)

    다음 주에서 잡을 수 있는 시간을 찾고 있습니다.
```

하단 버튼 없음.

버튼을 누른 시점에 이미 후보 계산은 끝나 있고, 보기를 `주` + 2026-08-03 주로 강제 이동시킵니다.

**1.5초 뒤 자동으로 다음 화면으로 넘어갑니다.**

| 조건 | 다음 화면 |
|---|---|
| 후보가 하나도 없음 | §8-1 후보 없음 화면 (3단계) |
| 후보가 있음 | 1순위 후보를 AI가 자동 선택하고 바로 §9 완료 화면 (4단계)로 건너뜀 |

즉 **정상 흐름에서 3단계(결과 목록) 화면은 건너뜁니다.** 4단계에서 `다른 시간으로` 를 눌렀을 때만 보입니다.

후보 자동 처리 규칙
- 선택된 후보의 순위가 1 또는 2 → 바로 확정 (`confirmed`)
- 순위가 3 또는 4 → 확정하지 않고 시간만 확보 (`ask`)

---

## 8. 시트 3단계 — 가능한 일정 (결과)

제목은 항상 `가능한 일정` 입니다.

### 8-1. 후보가 하나도 없을 때

```
가능한 일정
옮길 수 있는 시간까지 봤지만 후보가 없습니다.

옮길 수 없는 일정과 겹친 {12}칸을 빼고 나면, 남는 건
필수 3명 이상이 겹치는 시간뿐입니다.
필수를 줄이거나 범위를 다음다음 주로 넓혀야 합니다.

[ 인원 수정 ]
```

`인원 수정` → §6 으로 되돌아감.

### 8-2. 1·2순위 후보가 있을 때

```
가능한 일정
AI가 고른 시간입니다. 보낼지만 정하시면 됩니다.

전원이 비는 시간을 찾았습니다. 바로 확정 초대가 나갑니다.
```

### 8-3. 3·4순위 후보만 있을 때

```
가능한 일정
전원이 비는 시간이 없어, 물어볼 사람이 가장 적은 시간을 골랐습니다.

전원이 비는 시간은 없습니다. 여기서 멈추지 않고
옮길 수 있는 일정과 겹치는 시간까지 봤습니다.
“일정이 있다”를 확정된 불가능이 아니라 아직 물어보지 않은 상태로 취급합니다.
```

### 8-4. 추천 카드 (공통)

```
[{3}순위]  [AI 추천]

{8/4(화) 15:00–16:00}

{겹치는 일정이 1건뿐이고 모두 옮길 수 있는 일정입니다. 후보 중 물어볼 사람이 가장 적습니다.}

점심 직후 — 참석은 되지만 결정의 질이 떨어지는 시간입니다.

(아바타) {김도현} — 「{채용 면접}」  [옮길 수 없음]  [놓치면 다시 잡기 어려움]

{김도현}에게만 조율 요청이 갑니다.
나머지 {5}명에게는 아무 알림도 가지 않습니다.
```

**추천 사유 문구 (`고른 이유`)**

| 순위 | 문구 |
|---|---|
| 1 | `필수·선택 인원이 모두 실제로 비어 있는 시간 중 가장 이른 자리입니다.` |
| 2 | `필수 인원은 모두 가능하고, 선택 인원 {최민아, 한지우}만 겹칩니다. 물어볼 사람이 없습니다.` |
| 3·4 | `겹치는 일정이 {1}건뿐이고 모두 옮길 수 있는 일정입니다.` (+ 후보 중 겹침이 가장 적으면 이어서 ` 후보 중 물어볼 사람이 가장 적습니다.`) |

**점심 직후 경고** — 후보 시간이 13~14시에 걸칠 때만

```
점심 직후 — 참석은 되지만 결정의 질이 떨어지는 시간입니다.
```

**겹친 일정 태그 (`MOBILITY_LABEL`)**

| 판정 | 표시 |
|---|---|
| fixed | `옮길 수 없음` |
| soft | `옮길 수 있음` |
| unknown | `물어봐야 앎` |

판정 기준
- 참여자 3명 이상 → `옮길 수 없음`
- 강태섭이 낀 일정 → `옮길 수 없음`
- 제목이 `외부 미팅|고객|점심 미팅|임원 보고|이사회|대표 보고|면접|계약 서명|방문|워크숍|공유회|온보딩|인터뷰|고객 콜|분쟁|자문|약관|스탠드업|데일리` 에 걸림 → `옮길 수 없음`
- 제목이 `업무 집중|집중 근무|개인 작업|정리|작성|준비|점검|모니터링|처리|최적화|리팩터링|프로파일링|스크립트|서류|계획|승인|검토|테스트|리뷰` 에 걸림 → `옮길 수 있음`
- 그 외 → `물어봐야 앎`

`놓치면 다시 잡기 어려움` 은 겹친 사람이 강태섭일 때만 붙습니다.

**조율 요청 안내** — 겹친 사람이 있을 때만

```
{김도현}에게만 조율 요청이 갑니다. 나머지 {5}명에게는 아무 알림도 가지 않습니다.
```

### 8-5. 다른 후보

```
▸ 다른 후보 {4}개
```

펼치면 `▾ 다른 후보 {4}개` 로 바뀌고 아래에 목록이 나옵니다.

```
[{2}]  {8/5(수) 10:00–11:00}
       {필수 전원 가능 · 선택 최민아 불가}        이걸로
```

한 줄 설명(`alt-note`) 규칙

| 조건 | 문구 |
|---|---|
| 2순위이고 선택 인원이 겹침 | `필수 전원 가능 · 선택 {최민아} 불가` |
| 겹친 필수 인원이 있음 | `{김도현} 「{채용 면접}」` (여러 명이면 쉼표로 이음) |
| 그 외 | 순위별 고정 문구 (아래) |

순위별 고정 문구 (`RANK_TEXT`)

| 순위 | 문구 |
|---|---|
| 1 | `전원 실제로 비어 있음` |
| 2 | `필수 전원 가능 · 선택만 겹침` |
| 3 | `옮길 수 있는 일정 1건과 겹침` |
| 4 | `옮길 수 있는 일정 2건과 겹침` |

### 8-6. 하단

```
[ 인원 수정 ]                     [ 초대 + 리마인드 보내기 ]
```

| 선택된 후보 순위 | 우측 버튼 |
|---|---|
| 1 또는 2 | `초대 + 리마인드 보내기` |
| 3 또는 4 | `시간 확보 + 조율 요청` |

### 8-7. 분기

| 조작 | 결과 |
|---|---|
| `인원 수정` | §6 으로 |
| `이걸로` (다른 후보) | 그 후보를 선택하고 목록을 접은 뒤 같은 화면 다시 그림 |
| 캘린더 위 후보 블록(`{N}순위`) 클릭 | 그 후보를 선택하고 같은 화면 다시 그림 |
| `초대 + 리마인드 보내기` | §9-1 확정 완료 |
| `시간 확보 + 조율 요청` | §9-2 시간 확보 |

### 8-8. 이 화면에서 캘린더가 바뀌는 것

- 그리드 전체가 어두워지고, 후보 시간대에 순위 블록이 뜹니다: `{3}순위` / `{15:00}`
- 선택된 후보를 막고 있는 필수 인원의 일정에는 노란 실선 테두리, 선택 인원의 일정에는 노란 점선 테두리가 생깁니다.

---

## 9. 시트 4단계 — 결과 발송

### 9-1. 확정 완료 (1·2순위 또는 조율 성공)

```
확정 완료
전원이 비는 시간이라 바로 확정했습니다.

              ✓

       일정이 잡혔습니다
참석자 전원에게 캘린더 초대와 슬랙 리마인드가 발송되었습니다.

┌─────────────────────────────────────┐
│ 회의      {3분기 로드맵 우선순위 정렬}   │
│ 시간      {8/5(수) 10:00–11:00} · 확정 │
│ 고른 이유  {필수·선택 인원이 모두 …}     │
│ 초대      {김도현(필수), 이준호(필수), …} │
└─────────────────────────────────────┘

캘린더에 이렇게 바뀝니다
┌──────────────────────────┐
│ ✓ {3분기 로드맵 우선순위 정렬} │
│ 확정 · 참석자 {6}명          │
└──────────────────────────┘

참석자에게 가는 슬랙 리마인드
┌────────────────────────────────────────┐
│ ⚡ 자동 조율  [앱]  오후 6:14              │
│                                        │
│ 「{3분기 로드맵 우선순위 정렬}」 일정이 확정됐습니다. │
│ 📅 {8/5(수) 10:00–11:00}                │
│ 참석 {6}명 · 캘린더 초대를 함께 보냈습니다.     │
│                                        │
│ 시작 10분 전에 한 번 더 알려드립니다.         │
└────────────────────────────────────────┘

[ 다른 시간으로 ]                          [ 닫기 ]
```

부제는 어떻게 확정됐는지에 따라 갈립니다.

| 조건 | 부제 | 본문 |
|---|---|---|
| 1·2순위로 바로 확정 | `전원이 비는 시간이라 바로 확정했습니다.` | `참석자 전원에게 캘린더 초대와 슬랙 리마인드가 발송되었습니다.` |
| 조율 요청 후 확정됨 | `겹친 블록이 옮겨져서 가예약이 확정으로 바뀌었습니다.` | `{김도현}님이 블록을 옮겨서 확정됐습니다.` + 줄바꿈 + `참석자 전원에게 확정 초대가 나갔습니다.` |

제목이 비어 있으면 요약표에는 `(제목 없음)`, 미리보기 블록에는 `제목 없음`, 슬랙 카드 본문에는 `회의` 가 들어갑니다.

### 9-2. 시간 확보 + 조율 요청 (3·4순위)

```
시간 확보 + 조율 요청
아직 확정이 아닙니다. 응답을 받고 주최자가 확정합니다.

              ⏳

   확정이 아니라 자리를 잡아뒀습니다
겹친 블록의 주인 {1}명에게만 조율 요청이 갔습니다.
나머지 {5}명의 캘린더에는 미정으로 이 시간이 확보됐습니다.

전원 캘린더에 이렇게 뜹니다 — 오른쪽 {8/4(화) 15:00–16:00} 자리
┌──────────────────────────────────┐
│ [미정] {3분기 로드맵 우선순위 정렬}     │
│ 응답 대기 중 · 정슬기 · 수락 버튼 없음   │
└──────────────────────────────────┘

겹친 사람에게 가는 슬랙 ({1}명)
┌──────────────────────────────────────────┐
│ ⚡ 자동 조율  [앱]  오후 6:12                 │
│                                          │
│ @{김도현} 님, {3분기 로드맵 우선순위 정렬}을     │
│ {8/4(화) 15:00–16:00}에 잡으려고 합니다.      │
│ 그 시간에 「{채용 면접}」이 걸려 있어요.          │
│ 이 일정을 옮길 수 있나요?                     │
│                                          │
│ [ 가능 ]  [ 불가능 ]                        │
│                                          │
│ 답은 주최자(정슬기)에게만 갑니다.               │
│ 다른 참석자에게는 아무 알림도 가지 않습니다.       │
└──────────────────────────────────────────┘

나머지 {5}명에게 가는 것
아무 알림도 가지 않습니다. 캘린더에 미정 블록만 조용히 잡히고,
확정되면 그때 초대가 나갑니다.
묻는 대상을 {6}명에서 {1}명으로 좁히는 게 목적입니다.

┌──────────────────────────────────────────┐
│ 확보한 시간  {8/4(화) 15:00–16:00}  [미정]   │
│ 고른 이유    {겹치는 일정이 1건뿐이고 …}       │
│ 확정 조건    {김도현}님이 가능으로 답하면        │
│             주최자가 확정합니다.               │
│ 리마인드     24시간 뒤 자동 발송 ·             │
│             주최자가 지금 보낼 수도 있습니다.    │
└──────────────────────────────────────────┘

[ 다른 시간으로 ]                    [ 리마인드 보내기 ]
```

**슬랙 카드 경고줄** — 겹친 사람이 강태섭일 때만 카드 안에 추가

```
이 회의에는 재조율이 어려운 참석자가 있습니다. 놓치면 다시 잡기 어렵습니다.
```

**슬랙 카드 응답 표시** — `가능` / `불가능` 을 누르면 카드 아래에 붙습니다.

| 누른 버튼 | 표시 |
|---|---|
| `가능` | `{김도현} — 가능` / `블록을 옮길 수 있음` |
| `불가능` | `{김도현} — 불가능` / `옮길 수 없음` |
| 아직 안 누름 + 리마인드를 보낸 적 있음 | `주최자가 리마인드를 {1}회 보냈습니다 · 아직 답 없음` |

**요약표의 조건부 문구**

| 항목 | 조건 | 문구 |
|---|---|---|
| 확정 조건 | 물어볼 사람 1명 | `{김도현}님이 가능으로 답하면 주최자가 확정합니다.` |
| 확정 조건 | 물어볼 사람 2명 | `두 사람이 모두 가능으로 답하면 주최자가 확정합니다.` |
| 리마인드 | 아직 안 보냄 | `24시간 뒤 자동 발송 · 주최자가 지금 보낼 수도 있습니다` |
| 리마인드 | 보낸 적 있음 | `주최자가 {1}회 보냄 · 24시간 뒤 자동으로 한 번 더` |

### 9-3. 하단 버튼 분기

좌측은 항상 `다른 시간으로` 입니다. 우측 버튼은 응답 상태로 갈립니다.

| 상태 | 우측 버튼 | 누르면 |
|---|---|---|
| 이미 확정됨 | `닫기` | 시트를 닫고 토스트 `일정이 캘린더에 등록되었습니다.` |
| 한 명이라도 `불가능` | `다른 시간 다시 찾기` | 확보한 시간을 지우고 §8 결과 화면으로 |
| 전원 `가능` | `확정 초대 보내기` | 그 자리에서 §9-1 확정 완료로 바뀜 (리마인드 횟수 0으로 초기화) |
| 그 외 (응답 없음) | `리마인드 보내기` | 토스트 발송 후 시트가 닫힘 |
| 그 외 (이미 보낸 적 있음) | `리마인드 다시 보내기` | 위와 동일 |

`다른 시간으로` → 다른 후보 목록을 펼친 상태로 §8 결과 화면으로.

리마인드를 보내면 토스트: `{김도현}님에게 리마인드를 보냈습니다.`

시트를 `✕` 나 `Esc` 로 닫아도 **확보한 시간은 캘린더에 남습니다.**

---

## 10. 확보한 시간 팝오버

시트를 닫은 뒤, 캘린더에 남은 가예약/확정 블록을 클릭하면 뜹니다.

캘린더 위 블록 모양

| 상태 | 블록 문구 |
|---|---|
| 미확정 | `[미정] {3분기 로드맵 우선순위 정렬}` / `응답 대기 중 · 정슬기` |
| 확정 | `✓ {3분기 로드맵 우선순위 정렬}` / `확정 · 참석자 {6}명` |

**미확정 상태 팝오버**

```
              ☰   🗑   ✉   ⋮   ✕

■  [미정] {3분기 로드맵 우선순위 정렬}
   {8/4(화) 15:00–16:00}

👥  참석자 {6}명
💬  {김도현}님에게 조율 요청
    응답 대기 중
🔒  다른 참석자에게는 알림 없음 · 자리만 확보

[ 리마인드 보내기 ]        [ 확보 취소 ]
```

리마인드를 보낸 적이 있으면 `응답 대기 중` 자리에 `리마인드 {1}회 발송 · 아직 답 없음`, 버튼은 `리마인드 다시 보내기` 로 바뀝니다.

**확정 상태 팝오버**

```
■  ✓ {3분기 로드맵 우선순위 정렬}
   {8/5(수) 10:00–11:00}

👥  참석자 {6}명
✓   확정 · 초대 발송됨
```

확정 상태에는 하단 버튼이 없습니다.

| 조작 | 결과 |
|---|---|
| `리마인드 보내기` | 팝오버 닫힘 + 토스트 `{김도현}님에게 리마인드를 보냈습니다.` |
| `확보 취소` | 블록이 사라지고 토스트 `확보한 시간을 취소했습니다.` |

---

## 11. 둘러보기

`▶ 둘러보기` 를 누르면 시작됩니다. 화면을 덮지 않고, 대상 요소에 파란 링을 씌우고 옆에 작은 카드를 붙입니다.

카드 형태:

```
{1} / 12
{여기서 시작합니다}
{일정을 직접 만들지 않고 자동 조율을 씁니다. …}

● ○ ○ ○ ○ ○ ○ ○ ○ ○ ○ ○     건너뛰기   [ 다음 ]
```

마지막 단계에서는 `건너뛰기` 가 사라지고 버튼이 `직접 해보기` 로 바뀝니다.
단계 사이에 자동 조작이 실행되는 동안에는 카드에 `진행 중…` 만 표시됩니다.

시작할 때 시트를 닫고, 시나리오를 B(캘린더가 꽉 찬 주)로 맞추고, 2026-08-03 주로 이동합니다.

### 12단계 전문

**1 / 12** — 대상: `만들기` 버튼
> **여기서 시작합니다**
> 일정을 직접 만들지 않고 **자동 조율**을 씁니다. 팝업이 바로 뜨는 대신 만들기 메뉴 안에 들어 있습니다.
>
> `다음` → 만들기 메뉴를 엶

**2 / 12** — 대상: 메뉴의 `자동 조율`
> **자동 조율**
> 참석자를 고르면 다음 주에서 잡을 수 있는 시간을 대신 찾아줍니다.
>
> `다음` → 자동 조율 클릭 (시트 열림)

**3 / 12** — 대상: 링크 입력창
> **링크를 붙여넣으면 됩니다**
> 슬랙 스레드나 노션 문서를 넣으면 AI가 읽고 **참석자와 필수 / 선택**을 뽑습니다. 누가 와야 하는지 직접 고를 필요가 없습니다.
>
> `다음` → 슬랙 링크 분석 자동 실행 (2.6초 대기)

**4 / 12** — 대상: 참석자 첫 줄
> **근거가 함께 나옵니다**
> “스레드를 열고 결정을 요청함” 처럼 **왜 필수인지**가 줄마다 붙습니다. 주최자가 누르면 바로 바뀌고 “주최자 수정”으로 표시됩니다.

**5 / 12** — 대상: 화요일 칸
> **참석자 캘린더만 남습니다**
> 팀 전체가 아니라 이 회의와 관계있는 사람의 일정만 보입니다. 어느 시간이 왜 막혔는지가 그제야 읽힙니다.

**6 / 12** — 대상: `가능한 일정 뽑기` 버튼
> **가능한 일정 뽑기**
> “이 시간에 일정이 있는가”가 아니라 **“그 일정을 옮길 수 있는가”**로 훑고, 고르는 것까지 끝냅니다.
>
> `다음` → 스캔 실행 (1.9초 대기)

**7 / 12** — 대상: 완료 화면 제목
> **고르는 것도 보내는 것도 AI가**
> 전원이 비는 시간이 없으면 **옮길 수 있는 일정과 겹치는 시간**까지 보고, 물어볼 사람이 가장 적은 자리를 골라 바로 처리합니다. 확정이 아니라 **시간 확보**입니다.

**8 / 12** — 대상: 요약표
> **왜 이 시간인지 남깁니다**
> 고른 이유와 확정 조건이 함께 적힙니다. 마음에 안 들면 아래 **다른 시간으로**에서 바꿀 수 있습니다.

**9 / 12** — 대상: 슬랙 카드
> **이 슬랙이 한 명에게만 갑니다**
> 참여자가 낼 답은 **가능 / 불가능** 둘뿐입니다. 답이 없으면 주최자가 아래 **리마인드 보내기**를 누릅니다.

**10 / 12** — 대상: `리마인드 보내기` 버튼
> **주최자에게 남은 행동은 하나**
> 답이 없으면 **리마인드 보내기**. 누르면 시트가 닫히고, 확보한 시간이 캘린더에 남습니다.
>
> `다음` → 리마인드 보내기 클릭 (시트 닫힘)

**11 / 12** — 대상: 캘린더의 가예약 블록
> **캘린더에 미정으로 남습니다**
> 초대장이 아니라 가예약이라 아무도 수락·거절을 판단하지 않습니다. 블록을 누르면 상태를 보고 **리마인드**나 **확보 취소**를 할 수 있습니다.

**12 / 12** — 대상: `▶ 둘러보기` 버튼
> **끝났습니다**
> 슬랙 카드의 **가능**을 눌러 확정까지 이어보세요. 언제든 이 버튼으로 다시 볼 수 있습니다.
>
> 버튼: `직접 해보기` → 투어 종료

| 조작 | 결과 |
|---|---|
| `다음` | 다음 단계로 (자동 조작이 걸린 단계는 실행 후 이동) |
| `건너뛰기` | 투어 즉시 종료 |
| `직접 해보기` | 투어 종료 |
| 투어 중 `▶ 둘러보기` 다시 클릭 | 투어 종료 |
| 대상 요소를 화면에서 찾을 수 없음 | 그 단계를 건너뛰고 다음으로 |

---

## 12. 보기 전환

`주 ▾` 를 누르면 뜹니다.

```
보기
일          {✓} D
주          {✓} W
월          {✓} M
연도        {✓} Y
일정 목록    {✓} A
4일         {✓} X
─────────────────
주말 표시           {✓}
거절한 일정 표시     {✓}
```

체크 표시는 현재 선택된 항목에만 붙습니다. 토글 두 개는 켜져 있을 때 `✓` 가 붙습니다(`거절한 일정 표시` 는 처음부터 켜져 있음).

키보드 단축키로도 바꿀 수 있습니다 (입력창에 포커스가 없을 때).

| 키 | 동작 |
|---|---|
| `D` | 일 보기 |
| `W` | 주 보기 |
| `M` | 월 보기 |
| `Y` | 연도 보기 |
| `A` | 일정 목록 |
| `X` | 4일 보기 |
| `T` | 오늘로 이동 |
| `N` 또는 `J` | 다음 |
| `P` 또는 `K` | 이전 |

### 보기별 화면

**일 / 4일 / 주** — 시간표 형태. 열 개수만 다릅니다. 주 보기에서 `주말 표시` 를 켜면 토·일이 추가됩니다.

**월 보기**

```
월 화 수 목 금 토 일
┌────┬────┬────┬…
│ 3  │ 4  │ 5  │
│09:00 팀 스탠드업       │
│09:30 주간 계획         │
│10:00 업무 집중         │
│+5개 더                │
```

일정이 3개를 넘으면 `+{5}개 더` 로 줄여 표시합니다.

**연도 보기** — `1월` ~ `12월` 12개의 작은 달력. 일정이 있는 날은 글자가 굵어집니다.

**일정 목록** — 오늘부터 21일간의 목록.

```
 5     8월 수요일    오전 9:00 – 오전 10:00  ● 데일리        김도현
                    오전 10:00 – 오전 11:00 ● 캠페인 점검    최민아
```

일정이 하나도 없으면: `표시할 일정이 없습니다.`

월·연도·일정 목록 화면에서 날짜를 클릭하면 그 날의 **일 보기**로 이동합니다.

### 기간 제목 문구 규칙

| 보기 | 예시 |
|---|---|
| 일 | `2026년 8월 3일` |
| 주 · 4일 (같은 달) | `2026년 8월` |
| 주 · 4일 (달이 걸침) | `2026년 8월 – 9월` |
| 월 · 일정 목록 | `2026년 8월` |
| 연도 | `2026년` |

---

## 13. 상단바 메뉴

### 지원 (`?`)

```
도움말
학습 센터
업데이트
─────────
의견 보내기
```

### 설정 (`⚙`)

```
설정
휴지통
밀도 및 색상
─────────────
자동 조율 기본값   New
```

### 계정 (`정`)

```
(아바타)  정슬기
         seulgi@company.co.kr
─────────────────────────
다른 계정 추가
로그아웃
```

세 메뉴 모두 항목에 클릭 핸들러가 없습니다. 바깥을 클릭하면 닫힙니다.

---

## 14. 토스트 (스낵바)

화면 왼쪽 아래에 4.2초간 떴다가 사라집니다.

| 문구 | 뜨는 시점 |
|---|---|
| `{김도현}님에게 리마인드를 보냈습니다.` | 리마인드 발송 |
| `확보한 시간을 취소했습니다.` | 가예약 팝오버에서 `확보 취소` |
| `일정이 캘린더에 등록되었습니다.` | 확정 상태에서 `닫기` |
| `확보한 시간이 캘린더에 남아 있습니다.` | 미확정 상태에서 `닫기` |

---

## 15. 화면에 노출되지 않는 조작

`Shift + D` — 캘린더 밀도 시나리오 전환

| 시나리오 | 내용 |
|---|---|
| B (기본) | 빈 칸마다 방어 블록(`업무 집중` / `집중 근무` / `개인 작업`)을 채워 전원이 비는 시간이 없는 주 |
| A | 방어 블록 없이 여유가 있는 주 |

전환하면 스캔 결과가 초기화되고 시트는 1단계로 돌아갑니다.

---

## 16. 전체 흐름 요약

```
캘린더
  ├─ 빈 칸 클릭 → 「이 시간에」 팝오버 ─ 자동 조율 ─┐
  ├─ 일정 클릭 → 일정 상세 팝오버                  │
  ├─ 종일 칩 클릭 → 종일 팝오버                    │
  ├─ 가예약 블록 클릭 → 확보 시간 팝오버             │
  └─ 만들기 → 자동 조율 ──────────────────────────┤
                                                 ▼
                                    ① 인원 지정 (시트)
                                      링크 분석 → 참석자 자동 추출
                                      필수 1명 이상 → 「가능한 일정 뽑기」
                                                 ▼
                                    ② 스캔 중 (1.5초)
                                    ┌────────────┴────────────┐
                             후보 없음                    후보 있음
                                    ▼                          ▼
                          ③ 후보 없음 화면          ④ 결과 발송 (자동 선택)
                            「인원 수정」 → ①        ┌────────┴────────┐
                                                1·2순위            3·4순위
                                                   ▼                  ▼
                                              확정 완료         시간 확보 + 조율 요청
                                                   │            ┌──────┼──────┐
                                                   │         전원 가능  불가능  무응답
                                                   │            ▼       ▼       ▼
                                                   │       확정 초대  다시 찾기 리마인드
                                                   │          → 확정   → ③    → 시트 닫힘
                                                   ▼
                                              「닫기」 → 캘린더
                                              「다른 시간으로」 → ③ 결과 목록
```
